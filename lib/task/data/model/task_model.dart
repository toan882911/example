class TaskModel{
  String title;
  String date;
  String status;
  int icon;
  int container;
  int file;
  TaskModel(this.title, this.date, this.status, this.icon, this.container,
      this.file);

}